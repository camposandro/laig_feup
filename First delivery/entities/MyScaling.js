/**
 * MyScaling class, representing a scaling.
 */
class MyScaling {

	/**
     * @constructor
     * @param {x,y,z}
     */
    constructor(x, y, z) {
        this.vec = [x, y, z];
    }
}