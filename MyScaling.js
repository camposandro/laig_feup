/**
 * MyScaling class, representing a scaling.
 */
class MyScaling  {

	/**
     * @constructor
     * @param {x,y,z}
     */
    constructor(x,y,z) {
       this.x = x;
       this.y = y;
       this.z = z;
    }
}