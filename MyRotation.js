/**
 * MyRotation class, representing a rotation.
 */
class MyRotation  {

	/**
     * @constructor
     * @param {axis,angle}
     */
    constructor(axis,angle) {
       this.axis = axis;
       this.angle = angle;
    }
}