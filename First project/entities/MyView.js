/**
 * MyView class, representing a view.
 */
class MyView {

	/**
     * @constructor
     * @param {id, near, far} 
     */
	constructor(id, near, far) {
		this.id = id;
		this.near = near;
		this.far = far;
	}
}