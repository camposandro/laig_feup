/**
 * MyScaling class, representing a scaling.
 */
class MyScaling {

	/**
     * @constructor
     * @param {x} x Scaling-x-value
     * @param {y} y Scaling-y-value
     * @param {z} z Scaling-z-value
     */
    constructor(x, y, z) {
        this.vec = [x, y, z];
    }
}