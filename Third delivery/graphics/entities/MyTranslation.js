/**
 * MyTranslation class, representing a translation.
 */
class MyTranslation {

	/**
     * @constructor
     * @param {x} x Translation x-value
     * @param {y} y Translation y-value
     * @param {z} z Translation z-value
     */
    constructor(x, y, z) {
        this.vec = [x, y, z];
    }
}