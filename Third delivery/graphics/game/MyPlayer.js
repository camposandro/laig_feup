/**
 * MyPlayer, representing a player.
 * @constructor
 */
class MyPlayer {

    /**
     * @constructor
     * @param {id} id Player id (pieces colour)
     */
    constructor(id) {
        this.id = id;
    };
}